"""Model-specific loaders for supported MLLMs.

Each loader implements the :class:`~relcheck_v3.mllm.wrapper.ModelLoader`
protocol, handling weight downloading, model instantiation, and inference
for a single MLLM architecture.
"""
